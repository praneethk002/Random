export default function About() {
  return (
    <main style={{ padding: '2rem', maxWidth: 800, margin: '0 auto' }}>
      <h1>About FitOpt</h1>
      <p>
        FitOpt combines nutrition and workout planning through a stable API contract so the
        optimisation engine can evolve without breaking the user experience.
      </p>
    </main>
  );
}
